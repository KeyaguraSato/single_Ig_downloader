## how to use

### !you need python to use it!

- create a folder.
- move the files to the folder.
- use powershell to install "venv" in the folder.
  > cd (directory)
  > python -m venv venv
- now install ytb-dlp
  > pip install ytb-dlp
